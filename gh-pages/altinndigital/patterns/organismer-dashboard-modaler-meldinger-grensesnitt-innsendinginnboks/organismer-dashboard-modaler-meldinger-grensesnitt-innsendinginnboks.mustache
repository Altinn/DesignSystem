<div class="modal-content">
  <div class="modal-header a-modal-header">
      <div class="a-iconText a-iconText-background a-iconText-large">
          <div class="a-iconText-icon">
              <i class="ai ai-send
                a-icon" aria-hidden="true"></i>
          </div>
          <h1 class="a-iconText-text mb-0">
               {{# small-modal-title }}<span class="a-iconText-text-small">{{ small-modal-title }}</span>{{/ small-modal-title }}
              <span class="a-iconText-text-large">Bestill endring av grensesnitt</span>
          </h1>
      </div>
  </div>
  <div class="modal-body a-modal-body">
    
  <h2 class="a-h3 mb-2">Innsendingstjeneste til meldingsboks</h2>
  <form data-toggle="validator" id="contactForm">

    {{# tjenesteeier }}
      <div class="dashboard-prefilled">
        {{> felles-komponenter-tekst-input }}
      </div>
    {{/ tjenesteeier }}

    {{# akronym }}
      {{> felles-komponenter-tekst-input:mb-2 }}
    {{/ akronym }}

    {{# grensesnitt }}
      {{> felles-komponenter-tekst-input:mb-2 }}
    {{/ grensesnitt }}

    {{# existing }}
      <div class="mb-2">
        {{> felles-komponenter-avkrysningsboks }}
        <div id="eksisterende" class="collapse">
          {{# name }}
            {{> felles-komponenter-tekst-input }}
          {{/ name }}
        </div>
      </div>
    {{/ existing }}

    {{!-- Må velge én --}}
    {{> felles-komponenter-valgknapper-og-spesifiser:mb-2 }}

    {{# kodeliste }}
      {{> felles-komponenter-tekst-input:mb-2 }}
    {{/ kodeliste }}

    {{# orgnummer }}
      {{> felles-komponenter-tekst-input:mb-2 }}
    {{/ orgnummer }}

    {{# epost }}
      {{> felles-komponenter-tekst-input:mb-2 }}
    {{/ epost }}

    {{# information }}
      {{> felles-komponenter-tekstfelt:mb-2 }}
    {{/ information }}
    
    {{# submit}}
      {{> felles-komponenter-knapp:mb-2 }}
    {{/ submit}}
  </form>
</div>
