<article class="a-linkArticle" {{# identifier }}id="{{ identifier }}"{{/ identifier }}>

  {{{ heading-start }}}
  {{# linkTitle }}
    {{> felles-komponenter-link }}
  {{/ linkTitle }}
  {{{nolinkTitle}}}
  {{# heading-label }}
    {{> felles-komponenter-merkelapp }}
  {{/ heading-label }}
  {{{ heading-end }}}

  {{# breadcrumb }}
  <p class="a-searchResults-breadcrumb">{{{breadcrumb}}} </p>
  {{/ breadcrumb }}

  <p {{# maxLines }}class="{{ maxLines }}" {{/ maxLines }}>{{{ category-element-description }}}</p>
</article>
