<div class="a-page">
	{{> organismer-header }}

	<section id="content" tabindex="-1">

		<div class="container">
			<div class="row">
				<div class="col-sm-12 col-lg-10 offset-lg-1">
					{{# articlestart }}
					{{> felles-komponenter-artikkel-start }}
					{{/ articlestart }}
				</div>
			</div>

			{{# firstpart }}
			<div class="row">
				<div class="col-sm-12 col-lg-8 offset-lg-2 a-text epi-wysiwyg pt-2 pt-lg-3">

						<h2 class="a-h3">For å starte et enkeltpersonforetak må du:</h2>

						{{# list1 }}
							{{> felles-komponenter-liste-unummerert-kuler }}
						{{/ list1 }}

						<p>Er du usikker på om enkeltpersonforetak er riktig for deg kan du lese om <a href="#" class="a-linkArea"> valg av organisasjonsform </a> og skillet mellom <a href="#" class="a-linkArea">arbeidstakere og næringsdrivende</a>.</p>

						{{> felles-komponenter-seksjonstittel(section-title: "Navn på enkeltpersonforetak") }}

						<p>I enkeltpersonforetak må etternavnet ditt være en del av <a href="#" class="a-linkArea">foretaksnavnet</a>. Du kan i tillegg velge et markedsføringsnavn på bedriften. </p>

						{{> felles-komponenter-seksjonstittel(section-title: "Registrere enkeltpersonforetak") }}

						<h2 class="a-h3">Dersom innehaver har norsk fødselsnummer eller d-nummer:</h2>

						{{# list2 }}
							{{> felles-komponenter-liste-unummerert-sammenkoblet }}
						{{/ list2 }}

						{{# firstlink }}
						<strong>
							{{> felles-komponenter-link }}
						</strong>
						{{/ firstlink }}
						<br><br>

						<h2 class="a-h3">Dersom innehaver IKKE har norsk fødselsnummer eller d-nummer:</h2>

						{{# list3 }}
							{{> felles-komponenter-liste-unummerert-sammenkoblet }}
						{{/ list3 }}

						{{> felles-komponenter-seksjonstittel(section-title: "Hva koster det?") }}

						<p>Det er gratis å registrere enkeltpersonforetak. Alle enkeltpersonforetak blir registrert i Enhetsregisteret. Ved registrering får foretaket et organisasjonsnummer {{# footnote1 }} {{> felles-komponenter-link-med-popover-ikon }} {{/ footnote1 }}</p>

						{{> felles-komponenter-seksjonstittel(section-title: "Hvor lang tid tar det") }}

						{{# link2 }}
							{{> felles-komponenter-link }}
						{{/ link2 }}

						{{> felles-komponenter-seksjonstittel(section-title: "Drive enkeltpersonforetak") }}

						{{# link3 }}
							{{> felles-komponenter-link }}
						{{/ link3 }}

				</div><!-- END .col-... -->
			</div><!-- END .row -->
			{{/ firstpart }}

			<div class="row">
				<div class="col-sm-12 col-lg-8 offset-lg-2 a-text epi-wysiwyg my-4">

					{{> molekyler-fant-du-det-du-lette-etter }}

				</div><!-- END .col-... -->
			</div><!-- END .row -->

		</div> <!-- END Container -->

	</section>

	{{> organismer-footer }}
</div>
