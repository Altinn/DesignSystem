<header class="a-header {{{ subheader-color }}}">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <nav id="primary-nav" class="a-globalNav float-left">
          <a href="{{  }}" class="a-globalNav-logo{{# subheader-extraclass }} {{ subheader-extraclass }}{{/ subheader-extraclass }}"> {{> felles-komponenter-logo }} <span class="sr-only">Til forsiden</span></a>
          {{# linkFeatured }}
            {{> felles-komponenter-link }}
          {{/ linkFeatured }}
        </nav>
    		<ul id="secondary-nav" class="nav nav-pills a-header-options">
    			<li class="nav-item">
    				{{> felles-komponenter-innlogget }}
            {{# dropdown-personswitch }}
              {{> organismer-meny-personbytte }}
            {{/ dropdown-personswitch }}
    			</li>
    		</ul>
      </div>
    </div>        
  </div>
</header>