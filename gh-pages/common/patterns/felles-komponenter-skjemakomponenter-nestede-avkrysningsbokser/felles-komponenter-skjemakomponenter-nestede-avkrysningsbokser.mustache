
<div class="row" {{# checkbox-list-id }}id='{{ checkbox-list-id }}'{{/ checkbox-list-id }}>
  {{# firstLevelCheckboxes }}
    <div class="{{ firstLevelColClass }}">
      <hr class="m-0 mb-1 {{# hrExtraClass }} {{ hrExtraClass }}{{/ hrExtraClass }}">
      {{> felles-komponenter-avkrysningsboks }}
      {{# hasSecondLevel }}
        <div class="row a-pl-half"{{# secondLevelId }} id="{{ secondLevelId }}"{{/ secondLevelId }}>
          {{# secondLevelCheckboxes }}
            <div class="{{ secondLevelColClass }}">
              <div {{# thirdLevelCustomClass }} class="{{ thirdLevelCustomClass }}" {{/ thirdLevelCustomClass }}>
                {{> felles-komponenter-avkrysningsboks }}
              </div>
            </div>
          {{/ secondLevelCheckboxes }}
        </div>
      {{/ hasSecondLevel }}
    </div>
  {{/ firstLevelCheckboxes }}
  {{# loadmoreSearch }}
    <div class="{{ firstLevelColClass }}">
        <hr class="m-0 mb-1 {{# hrExtraClass }} {{ hrExtraClass }}{{/ hrExtraClass }}">
        {{> felles-komponenter-link-ikon }}
    </div>
  {{/ loadmoreSearch }}
  {{^ loadmoreSearch }}
    <div class="{{ firstLevelColClass }}">
        <hr class="m-0 mb-1 {{# hrExtraClass }} {{ hrExtraClass }}{{/ hrExtraClass }}">
    </div>
  {{/ loadmoreSearch }}
</div>
