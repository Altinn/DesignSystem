<nav class="a-inline-page-link">
  <div class="row">
    <div class="col-sm-12 col-md-6 list">
      {{# nav-elements-first }}
      <a href="{{ url }}" class="nav-link {{# disabled }} disabled {{/ disabled }}" alt="{{ alttext }}">{{ linktext }}</a> {{/ nav-elements-first }}
      <span class="hidden-sm-down"></span>
    </div>
    <div class="col-sm-12 col-md-6 list">
      {{# nav-elements-second }}
      <a href="{{ url }}" class="nav-link {{# disabled }} disabled {{/ disabled }}" alt="{{ alttext }}">{{ linktext }}</a> {{/ nav-elements-second }}
    </div>
  </div>
</nav>