<div class="a-iconText {{ variation-class }} {{ styleModifier }}">
  <!-- ICON -->
  <div class="a-iconText-icon">{{# hasIcon }}{{> atomer-ikon }}{{/ hasIcon }}{{# hasDate }}{{> atomer-dato }}{{/ hasDate }}</div>
  <!-- TEXT -->
  {{# isHeader }}
    <h1 class="a-iconText-text mb-0">
  {{/ isHeader}}
  {{^ isHeader }}
    <div class="a-iconText-text">
  {{/ isHeader }}
  {{# hasTextSmall }}
    <span class="a-iconText-text-small">{{ textSmall }}</span>
  {{/ hasTextSmall }}
  <span class="a-iconText-text-large">{{ icontext }} {{{ badge }}} </span>
  {{# isHeader }}
    </h1>
  {{/ isHeader}}
  {{^ isHeader }}
    </div>
  {{/ isHeader }}
</div>
