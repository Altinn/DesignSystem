<nav class="nav nav-inline a-inline-page-link">
  <div class="list">
    {{# nav-elements-first }}
      <a href="{{ url }}" class="nav-link {{# disabled }} disabled {{/ disabled }}" alt="{{ alttext }}">{{ linktext }}</a>
    {{/ nav-elements-first }}

    {{# nav-elements-second }}
      <a href="{{ url }}" class="nav-link {{# disabled }} disabled {{/ disabled }}" alt="{{ alttext }}">{{ linktext }}</a>
    {{/ nav-elements-second }}
  </div>
</nav>