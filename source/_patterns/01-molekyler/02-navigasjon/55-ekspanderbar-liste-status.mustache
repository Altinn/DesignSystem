<div class="a-listWithSubLevels">
  {{# button-main }}
    {{> atomer-knapp-ikon-tekst }}
  {{/ button-main }}

  {{# button-sub }}
    <div class="a-list-heading">
      {{> atomer-ekspanderbar-knapp }}
    </div>

    <div id="{{ collapse-header-contentID }}" class="a-collapseContent a-bgBlueLight a-shadow collapse {{# isVisible }}show{{/ isVisible}}" aria-expanded="{{ aria-expanded }}" {{# role }} role="{{ role }}" {{/ role }}>
      {{> molekyler-liste-unummerert-ny }}
     </div>

   {{/ button-sub }}

 </div>
