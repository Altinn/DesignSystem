<div class="a-page" id="smoothState">
  {{> organismer-header }}
  <div class="container">
    <div class="row">
      <div class="col-12">
	      <h1 class="mb-3 mt-0 a-fontSizeL">
          {{{ profile-login-prefix }}}
          <span class="a-fontBold">
            {{{ profile-login-name }}}
          </span>
          {{{ profile-login-id }}}
        </h1>
        <div id="{{ accordion-large-id }}" class="a-panelAccordion" role="tablist" aria-multiselectable="true">
          {{# your-contact-info-panel }}
            {{> organismer-din-kontaktinformasjon }}
          {{/ your-contact-info-panel }}
          {{# login-info-panel }}
            {{> organismer-innloggingsinformasjon }}
          {{/ login-info-panel }}
          {{# andreMedRettigheter }}
            {{> organismer-andre-med-rettigheter }}
          {{/ andreMedRettigheter }}
          {{# klientdelegering }}
            {{> organismer-klientdelegering }}
          {{/ klientdelegering }}
        </div>
      </div>
    </div>
  </div>
</div>