{{# isModal }}
  <div class="modal-body a-modal-body">
{{/ isModal }}
  <div class="row">
    <div class="col-12 pb-2">
      {{# section-title }}
        {{> atomer-seksjonstittel }}
      {{/ section-title }}
      {{> atomer-intro-tekst:d-inline }}
      {{# varslingsadresse-information-popover }}      
        {{> atomer-link-med-popover-ikon }}
      {{/ varslingsadresse-information-popover }}
    </div>
    <div class="col-12 col-md-7 pr-md-0" id="">
      {{# shared-notificationinfo-epost-input }}
        {{> atomer-tekst-input }}
      {{/ shared-notificationinfo-epost-input }}
    </div>
    <div class="col-12 mt-1">
      {{# hasAddEmailLink }}
        {{> atomer-knapp:mr-1 }}
      {{/ hasAddEmailLink }}
      {{# hasAddSMSLink-first }}
        {{> atomer-knapp }}
      {{/ hasAddSMSLink-first }}
    </div>
    {{# additionalinfo-sharednotification }}
    <div class="row d-none" {{# id}}id={{ id }}{{/ id}}>
      <div class="col-12 col-md-7 pr-md-0 mt-2">
        {{# shared-notificationinfo-phone-input }}
          {{> atomer-tekst-input }}
        {{/ shared-notificationinfo-phone-input }}
      </div>
      <div class="col-12 col-md-7 pr-md-0 mt-1">
        {{# hasAddSMSLink-second }}
          {{> atomer-knapp }}
        {{/ hasAddSMSLink-second }}
      </div>
    </div>
    {{/ additionalinfo-sharednotification }}
    <div class="col-12 mt-2">
      {{# save-notificationaddress-button }}
        {{> atomer-knapp:mr-2 }}
      {{/ save-notificationaddress-button }}
      {{# cancel-notificationaddress-button }}
        {{> atomer-knapp }}
      {{/ cancel-notificationaddress-button }}
    </div>
  </div>
{{# isModal }}
  </div>
{{/ isModal }}