{{# pre-header }}
  <div class="row">
    <div class="{{ pre-header-classes }}">
      {{> atomer-seksjonstittel }}
    </div>
  </div>
{{/ pre-header }}
<div class="{{^ is-static }}card a-accordion-card {{/ is-static }}a-collapsePanel{{# isLarge }} {{{ isLarge }}}{{/ isLarge }} {{{ styleModifier }}}">
  <div class="card-header {{^ is-static }}a-js-index-heading{{/ is-static }} a-collapsePanel-heading collapsed{{# isExpanded }} {{ isExpanded }}{{/ isExpanded }}{{# panel-extra-class }} {{ panel-extra-class }}{{/ panel-extra-class }}"{{^ is-static }}{{# collapse-panel-header-id }} id="{{ collapse-panel-header-id }}"{{/ collapse-panel-header-id }} role="tab"{{/ is-static }}>
    {{# direct-link-url }}
      <a href="{{ direct-link-url }}" class="collapsed">
    {{/ direct-link-url }}
    {{^ direct-link-url }}
      {{^ is-static }}
        <a href="#" data-toggle="collapse" data-target="#{{{ collapseID }}}" aria-expanded="false" aria-controls="{{ collapseID }}">
      {{/ is-static }}
    {{/ direct-link-url }}
      {{# collapseHeader }}
        {{^ isElement }}
          {{> molekyler-ikon-tekst }}
        {{/ isElement }}
        {{# isElement }}
          {{> molekyler-innboks-header-innhold }}
        {{/ isElement }}
      {{/ collapseHeader }}

    {{# direct-link-url }}
      </a>
    {{/ direct-link-url }}
    {{^ direct-link-url }}
      {{^ is-static }}
        </a>
      {{/ is-static }}
    {{/ direct-link-url }}
  </div>
  <div class="a-collapsePanel-body collapse{{# isVisibleFromStart }} {{ isVisibleFromStart }}{{/ isVisibleFromStart }}{{# extraPanelBodyClass}} {{ extraPanelBodyClass }}{{/ extraPanelBodyClass }}" {{# dataParent }}data-parent="{{{dataParent}}}"{{/ dataParent }} {{^ is-static }} role="tabpanel" aria-labelledby="{{ collapse-panel-header-id }}" id="{{ collapseID }}"{{/ is-static }}>
    {{# top-notification }}
      <div class="col a-collapsePanel-body-secondary a-no-sideborder">
        <p class="{{ notification-text-class }}">{{{ notification-text }}}</p>
        {{# text-link }}
          {{> atomer-link }}
        {{/ text-link }}
      </div>
    {{/ top-notification }}
    <div class="card-block a-text a-collapsePanel-body-content a-collapseContent-delay">
      {{^ messageBody }}
        <div class="row">
          <div class="col-12">
            {{{ collapse-panel-content }}}
          </div>
        </div>

      {{/ messageBody }}
      {{# messageBody }}
        {{> organismer-innboks-panel-innhold }}
      {{/ messageBody }}
    </div>
  </div>
</div>
