<div class="a-page">
  {{# header }}
    {{> organismer-header-altinnett }}
  {{/ header }}

  <section id="content" tabindex="-1">
    <h1 class="sr-only">Velkommen til Altinn Digitalisering!</h1>

    {{# hero }}
      {{> molekyler-jumbotron }}
    {{/ hero }}

    <div class="container">{{> molekyler-altinnett-kontaktkort }}</div>

    {{# product-presentation }}
      <div class="container">
        <div class="row">
          <div class="col-12 mb-4">
            {{> organismer-altinnett-produkt-presentasjon }}
          </div>
        </div>
      </div>
    {{/ product-presentation }}

    {{# case }}
      {{> molekyler-jumbotron }}
    {{/ case }}

    <div class="container">
      {{# steps }}
        <div class="row">
          <div class="col-12">
            <h2 class="a-fontBold a-blueDarkerText mb-2">{{ section-title }}</h2>

            {{# list }}
              <div class="d-block d-lg-none d-xl-none d-xxl-none">
                  {{> atomer-liste-nummerert-sirkel }}
              </div>
            {{/ list }}

            <div class="epi-wysiwyg d-none d-lg-block d-xl-block d-xxl-block">
              {{> molekyler-altinnett-stegvis-svg }}
            </div>

            {{# learnMoreLink }}
              <div class="mt-2 mb-8">
                {{> atomer-link }}
              </div>
            {{/ learnMoreLink }}
          </div>
        </div>
      {{/ steps }}

      {{# calendarList }}
        <div class="row">
          <div class="col-12">
            {{> organismer-artikkelliste-dato:mb-3 }}
          </div>
        </div>
      {{/ calendarList }}

      {{# loggedOut }}
        <div class="row">
          <div class="col-sm-12 col-lg-8 offset-lg-2 a-text epi-wysiwyg mt-1">
            <h2>Varslinger</h2>
          </div>

          <div class="col-sm-12 col-lg-10 offset-lg-1 a-text epi-wysiwyg">
            {{# previousAlerts }}
              {{> molekyler-altinnett-liste-varslinger }}
            {{/ previousAlerts }}
          </div>
        </div>

        <div class="row">
          <div class="col-sm-12 col-lg-8 offset-lg-2 a-text epi-wysiwyg mt-1">
            <h2>Abonnere på varslinger</h2>
            <p>Her kan du registrere din e-post adresse dersom du ønsker å motta viktige varslinger fra Altinn. Du bestemmer selv hvilke kategorier av hendelser du ønsker å motta varslinger om.</p>

            {{# subscribeLink }}
              {{> atomer-link }}
            {{/ subscribeLink }}
          </div>
        </div>

        {{# subscribeModal }}
          {{> organismer-modal }}
        {{/ subscribeModal }}
      {{/ loggedOut }}

      {{# tool-presentation }}
        <div class="row">
          <div class="col-12 mt-4">
            {{> organismer-altinnett-verktoy-presentasjon }}
          </div>
        </div>
      {{/ tool-presentation }}
    </div>

    {{# innovation }}
      <div class="container">
        <div class="row">
          <div class="col-12">
            <h2 class="a-fontBold a-blueDarkerText mb-3">{{ section-title }}</h2>
          </div>
        </div>
      </div>

      {{> molekyler-jumbotron }}
    {{/ innovation }}
  </section>

  {{# footer }}
    {{> organismer-footer-altinnett }}
  {{/ footer }}
</div>
